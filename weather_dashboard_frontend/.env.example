# Environment variables for Weather Dashboard (React)

# OpenWeatherMap API Key (required for fetching weather)
REACT_APP_OPENWEATHER_API_KEY=

# Optional: port and other template vars already present in platform
REACT_APP_PORT=3000
